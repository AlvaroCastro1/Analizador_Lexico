#include <stdio.h>
:)
int main() {
    // Definir una variable de tipo int
    int numero = 6; // Puedes cambiar este valor para probar diferentes casos
    @algo
    // Comprobar si el número es mayor a 5
    if (numero > 5) {
        // Acceder a un ciclo for para imprimir el número 5 tres veces
        for (int i = 0; i < 3; i++) {
            printf("%d ", 5);
        }
        printf("\n");
    }

    return 0;
}
