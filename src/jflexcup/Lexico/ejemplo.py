read(algo)
i++;
resultado = (num > 5) ? 100 : 200;


for (i = 1; i <= 5; i++) {
        if (i % 2 == 0) {
            printf("%d es par\n", i);
        } else {
            printf("%d es impar\n", i);
        }
    }
// Definición de la clase Calculadora
/*
*holas
*/
#include <stdio.h>
asd !=0
a1 = miVector[]
a2 = "Hola mundo"

// Definición de la clase Calculadora
typedef struct {
    int numero1;
    int numero2;
} Calculadora;

// Método para sumar dos números
int sumarNumeros( Calculadora calculadora ) {
    return calculadora.numero1 + calculadora.numero2;
}

int main() {
    // Crear una instancia de la clase Calculadora
    Calculadora miCalculadora;

    // Asignar valores a los números
    printf('Ingrese el primer número: ');
    scanf("%d", &miCalculadora.numero1);
    printf("Ingrese el segundo número: ");
    scanf("%d", &miCalculadora.numero2);

    // Llamar al método sumarNumeros con la instancia de la clase como argumento
    int resultado = sumarNumeros(miCalculadora);

    // Mostrar el resultado
    printf("La suma de los dos números es: %d\n", resultado);

    return 0;
}
